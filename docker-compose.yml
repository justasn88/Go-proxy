version: '3.8'

services:
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass asd
    ports:
      - "6379:6379"

  proxy-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis